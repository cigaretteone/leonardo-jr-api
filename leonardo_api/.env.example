# Leonardo Jr. API 環境変数サンプル
# このファイルを .env にコピーして値を設定すること
# .env は絶対にリポジトリにコミットしないこと

# DB（PostgreSQL）
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/leonardo_jr_dev

# JWT（本番では長いランダム文字列に変更すること）
JWT_SECRET_KEY=change-me-in-production-use-long-random-string
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=30

# デバイス認証（実証機用固定シークレット）
FACTORY_SECRET=LEONARDO_JR_2026_SECRET

# SMTP（メール通知）
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-gmail@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@leonardo-jr.jp

# IPジオロケーション閾値（km）
LOCATION_MISMATCH_THRESHOLD_KM=150.0
