[pytest]
# pytest-asyncio: 全 async def test_* を自動的に非同期テストとして扱う
asyncio_mode = auto

# カスタムマーカー定義
markers =
    integration: 実 PostgreSQL DB が必要な結合テスト。TEST_DATABASE_URL 環境変数を設定して実行。

# テスト探索パス
testpaths = tests device/tests

# ログ設定（テスト実行中の WARNING 以上を表示）
log_cli = true
log_cli_level = WARNING
